# CSC联盟自用 EE.log 分析器

纯前端单页应用：拖拽/点击上传 `EE.log`，在浏览器内解析并展示三类任务的精确时间指标。**零依赖、可离线使用**。

## 使用方法

直接双击打开 `index.html`（或在任意静态服务器托管的本目录），把 `EE.log` 拖入页面即可。
游戏日志默认位置：`%LOCALAPPDATA%\Warframe\EE.log`，可复制一份之后，上传复制的那一份EE.log文件
（也就是，尽量不要在游戏运行期间占用EE.log文件，防止被游戏视为作弊）。

## 功能

一目了然，没必要多说了。