# EdgeOne Pages 静态资源缓存规则（语法与 Cloudflare Pages 的 _headers 一致，EdgeOne Pages 原生支持）。
#
# data/*.json：物品检索索引(wm-items.json/drops-index.json)、世界状态镜像快照
# (ws-intl-zh.json/ws-cn-zh.json)等由 GitHub Actions 定期写回仓库的"预先生成好的
# 静态数据"，本来就该交给 CDN 长期缓存而不是每次都回源——这正是点5里要的"可搜索
# 索引库 + 缩略图都走 CDN 长期缓存，多页面复用"的落地方式之一。
#   - s-maxage=86400：边缘节点缓存 24 小时，命中率收益最大的一档。
#   - max-age=600：浏览器本地只信 10 分钟，避免数据更新后用户长时间看到旧值。
#   - stale-while-revalidate=3600：缓存过期后先吐旧值，后台悄悄换新，不让用户等。
# 注意：EdgeOne Pages 每次新部署都会整体刷新该次构建产物的边缘缓存，旧版本数据
# 不会因为这条缓存规则而"赖" CDN 不更新。
/data/*.json
  Cache-Control: public, max-age=600, s-maxage=86400, stale-while-revalidate=3600

# data/icons/*：实际下载缓存进本仓库的物品缩略图文件（见 cache_item_icons.py /
# cache-item-icons.yml），文件名本身已经带版本含义(WM图标文件名、wiki条目名)，同名
# 文件内容不会变，可以放心长期不过期缓存，不需要像 json 数据那样留短revalidate窗口。
/data/icons/*
  Cache-Control: public, max-age=2592000, s-maxage=2592000, immutable

# 注：另有一部分物品图标走的是 wm.wfspeed.run/static/*（EdgeOne 边缘函数自己的缓存
# 逻辑，见 edgeone-function.js），那是本地缓存没命中时的兜底路径，不是本 Pages 站点下
# 的路径，这里不需要也不应该重复配置。
